// Rock paper scissors game.

var game = this;
var choices = ["rock", "paper", "scissors"];
var Cchoice = var item = items[Math.floor(Math.random()*choices.length)];
var UserChoice = prompt('Rock, Paper, or Scissors?')

var computer = function(choice, compchoice){
  choice = Cchoice
}
